@startuml

(*) --> "Receive a request to find paginated users with manager role"

if "Request: findPaginatedUsersWithManagerRole" then
    --> "Retrieve all users with manager role from the database"
    --> "Paginate the user list based on the provided page and limit"
    --> "Return the paginated result"
    --> (*)
else
    --> "Invalid request"
    --> "Return an empty page"
    --> (*)
endif

@enduml
